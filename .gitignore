node_modules
dist/
docs/
*.log
.DS_Store
test/.artifacts/

# `npm pack` output — a local verification artifact, never a repo file.
*.tgz

# Previously-published SDK tarballs unpacked by scripts/check-previous-sdk-compat.mjs
.compat-sdk/
.selfhost-check/

# check-build-reproducible.mjs build dirs (renamed dist + scratch); cleaned by the script
.build-reproducible-*
